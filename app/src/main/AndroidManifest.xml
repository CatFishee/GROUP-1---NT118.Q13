<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.metube">

    <!-- Internet permissions for Firebase, Google, Facebook -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeTube"
        tools:replace="android:theme">

        <!-- Facebook SDK initialization -->
        <provider
            android:name="com.facebook.internal.FacebookInitProvider"
            android:authorities="com.facebook.app.FacebookContentProvider846187834526871"
            android:exported="false"
            tools:replace="android:authorities" />

        <!-- Facebook configuration -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <!-- Facebook Login Activity -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:theme="@style/com_facebook_activity_theme"
            tools:replace="android:theme" />

        <!-- Facebook Custom Tabs -->
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- LOGIN activity launches first -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN app activity -->
        <activity android:name=".ui.home.HomepageActivity" android:exported="true" />
        <activity android:name=".ui.upload.UploadActivity" android:exported="true" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ui.search.SearchActivity" android:exported="true" />
        <!-- Video playback activity -->
        <activity
            android:name=".ui.video.VideoActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="true" />
        <activity
            android:name=".ui.history.HistoryActivity"
            android:exported="false" />
        <activity android:name=".ui.playlist.PlaylistsActivity" />
        <activity
            android:name=".ui.playlist.PlaylistDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.playlist.EditPlaylistActivity"
            android:exported="false" />
        <activity android:name=".ui.downloads.DownloadsActivity" />
        <activity android:name=".ui.video.YourVideosActivity" />
        <activity
            android:name=".ui.video.EditYourVideoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.userstat.UserStatisticsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings.GeneralSettingsActivity"
            android:exported="false" />
    </application>
</manifest>